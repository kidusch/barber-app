const BASE_URL = 'http://barber-dashboard.abyssinia-vs.ch';

export const API_ENDPOINTS = {
  register: `${BASE_URL}/api/register`,
  login: `${BASE_URL}/api/login`,
  // Add more endpoints as needed
}; 